# AgentPay Guard Contract

Odra contract placeholder for the future Casper Testnet proof path.

The first real implementation should stay narrow:

- record `paymentId`
- record `requestHash`
- record payment status
- reject duplicate settlement
- emit events that CSPR.cloud can index

Do not implement production custody until the mock HTTP 402 flow and dashboard audit trail are working.

